{
  "name": "nutriscan-static",
  "version": "1.0.0",
  "description": "Static site with environment variable injection for Railway.",
  "main": "index.html",
  "scripts": {
    "build": "node setup-env.js",
    "start": "serve -s .",
    "dev": "node setup-env.js && serve"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}

